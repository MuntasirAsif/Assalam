import 'package:flutter/material.dart';

class OiblaScreen extends StatelessWidget {
  const OiblaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Oibla'),),
    );
  }
}
